<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Imepac"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".TelaPrincipal"
            android:exported="true"
            android:label="Imepac"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Tela_Perfil"
            android:exported="true"
            android:label="Imepac"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".FormCadastro"
            android:exported="true"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".FormLogin"
            android:exported="true"
            android:label="Imepac"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Imepac" />

        <!-- Adicionando a FichaTreinoActivity -->
        <activity
            android:name=".FichaTreino"
            android:exported="true"
            android:label="Ficha de Treino"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>
